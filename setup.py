#!/usr/bin/env python
"""
Setup script for easy-asr-server

This is a shim for backwards compatibility with older Python packaging tools.
The actual configuration is in pyproject.toml.
"""

import setuptools

if __name__ == "__main__":
    setuptools.setup() 